# 📚 Delta Shelf — Online Library (Flutter + SQLite)

**Delta Shelf** — учебное мобильное приложение на **Flutter**, реализующее систему учёта книг в библиотеке с использованием **локальной базы данных SQLite**.  
Проект демонстрирует работу с БД, бизнес-логикой, валидацией данных, управлением состоянием через `Provider` и полноценным пользовательским интерфейсом.

---

## Назначение проекта

Приложение предназначено для:
- хранения информации о книгах, авторах и читателях
- выдачи и возврата книг
- отслеживания просрочек
- поиска по библиотеке

**Проект выполнен в рамках учебной работы и не использует серверную часть — все данные хранятся локально.**

---

## Используемые технологии

- **Flutter**
- **Dart**
- **SQLite** (`sqflite`)
- **Provider** (state management)
- **path**
- **logger**

---

## Архитектура проекта

Проект разделён на логические слои:

### 1. Database
**`LibraryDatabase`**
- инициализация SQLite
- создание таблиц
- управление подключением к БД

Таблицы:
- `authors`
- `books`
- `readers`
- `loans`

---

### 2. Core (бизнес-логика)
**`LibraryCore`**
- выдача и возврат книг
- проверка доступности книги
- вычисление просрочки
- умный поиск (`smartSearch`)
- CRUD-операции для книг, авторов и читателей

---

### 3. Controller
**`BookController`**
- связывает UI и Core
- хранит состояние приложения
- обрабатывает ошибки
- уведомляет UI через `ChangeNotifier`

---

### 4. Validation
**`Validation`**
- проверка входных данных
- защита от некорректных id и пустых строк

---

### 5. UI (Flutter)
- вкладки (BottomNavigationBar)
- диалоги добавления / редактирования
- выдача и возврат книг
- визуальная индикация статуса книги и просрочки

---

## Структура базы данных

### `authors`
| Поле | Тип |
|-----|-----|
| id | INTEGER (PK) |
| full_name | TEXT |

### `books`
| Поле | Тип |
|-----|-----|
| id | INTEGER (PK) |
| title | TEXT |
| author_id | INTEGER (FK) |
| status | TEXT (`on_shelf` / `loaned`) |

### `readers`
| Поле | Тип |
|-----|-----|
| id | INTEGER (PK) |
| name | TEXT |

### `loans`
| Поле | Тип |
|-----|-----|
| id | INTEGER (PK) |
| book_id | INTEGER (FK) |
| reader_id | INTEGER (FK) |
| loan_date | TEXT |
| return_date | TEXT / NULL |

---

## Навигация приложения

Нижнее меню (**BottomNavigationBar**):

1. **Книги**
   - просмотр списка книг
   - добавление / редактирование / удаление
   - выдача и возврат
   - отображение статуса и просрочки

2. **Авторы**
   - добавление
   - редактирование
   - удаление

3. **Читатели**
   - добавление
   - редактирование
   - удаление

4. **Поиск**
   - поиск по названию книги
   - поиск по автору
   - поиск по имени читателя

---

## Поиск

Реализовано два уровня поиска:
- **UI-поиск** (фильтрация в `SearchTab`)
- **SQL-поиск** (`smartSearch`) с объединением таблиц:
  - книги
  - авторы
  - читатели

---

## Выдача и возврат книг

- книга может быть выдана только если её статус `on_shelf`
- при возврате:
  - фиксируется дата
  - вычисляется просрочка
- просрочка отображается:
  - в списке книг
  - в диалоге возврата

**Период выдачи** — 1 день (умышленно сокращён для тестирования)

---

## Запуск проекта

```bash
git clone https://github.com/detla4796/online_library4796
cd online_library4796
flutter pub get
flutter run
```

## License

Этот проект является проектом с открытым исходным кодом и распространяется под лицензией MIT.

## Автор

**Ушаков Данил detla4796**
