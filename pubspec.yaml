name: online_library
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  path: ^1.9.0
  provider: ^6.0.5
  logger: ^1.1.0
  share_plus: ^7.2.1

flutter:
  uses-material-design: true
